# cloud-build-ip-check-workerpool
A Cloud Build configuration file that utilizes a private worker pool (just-pool) to execute a curl command to print the external IP address used by the worker pool's build agent.
